Installation
============

The preferred way to install this extension is through `composer <https://getcomposer.org/download/>`__.

Either run

``php composer.phar require --prefer-dist beastbytes/mermaid-state-diagram``

or add

``"beastbytes/mermaid-state-diagram": "{{versionConstraint}}"``

to the ``require`` section of your composer.json.
